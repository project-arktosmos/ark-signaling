# Set to 'true' to disable UI and run signaling server only
# When enabled, all HTTP routes return 503, only WebSocket at /ws works
DISABLE_UI=false

# Server port (optional, defaults to config value or 6742)
PORT=6742
